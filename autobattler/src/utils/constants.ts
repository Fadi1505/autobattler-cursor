/** Logical width of the game canvas, before scaling. */
export const GAME_WIDTH = 1280;

/** Logical height of the game canvas, before scaling. */
export const GAME_HEIGHT = 720;

/** DOM id of the parent element that hosts the Phaser canvas. */
export const GAME_PARENT_ID = "game";

/** Background color of the game (matches the page background). */
export const GAME_BG_COLOR = "#0b0e14";
